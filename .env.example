# Framework Configuration
# All settings are optional and have sensible defaults

# Custom directory paths (defaults shown)
# TEST_SUITES_DIR=test-suites
# TESTS_AUTO_DIR=tests/automated
# TESTS_MANUAL_DIR=tests/manual
# TESTS_ARCHIVED_DIR=tests/archived

# Metrics (saved to .gitignore'd file)
# METRICS_ENABLED=false
# METRICS_FILE=.metrics.json
